kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: demo-cluster
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # frontend NodePort
        hostPort: 8080
        protocol: TCP
